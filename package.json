{
  "name": "export-incentive-assurance-platform",
  "private": true,
  "packageManager": "pnpm@9.12.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.14",
    "typescript": "^5.7.3"
  }
}
